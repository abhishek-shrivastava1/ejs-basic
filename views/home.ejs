<%- include('partials/header') -%>
    <h1>Home</h1>
    <p>
        <%= paraText  %>
    </p>
    <% for (let index = 0; index < posts.length; index++) { %>
        <h1>
            <%= posts[index].title %>
        </h1>
        <p>
            <%= posts[index].post.substring(0,100)+"..." %>
                <a href="/post/<%= posts[index].title %> ">Read more</a>
        </p>

        <% } %>
            <%- include('partials/footer') -%>